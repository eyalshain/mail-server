This project implements a simple mail server and client in Python.
The server receives messages with optional image and file attachments, while the client allows users to compose and send these messages.

